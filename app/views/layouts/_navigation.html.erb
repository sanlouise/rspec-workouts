<nav>
  <div class="nav-wrapper blue darken-3">
    <a href="/" class="brand-logo center">The Athletes Crib</a>
    <ul id="nav-mobile" class="right hide-on-med-and-down">
      
      <%= form_tag(search_dashboard_path, role: "search") do %>
      <div class="input-field">
        <%= text_field_tag :search_name, nil, placeholder: "Search for friends" %>
      </div>
        <%= submit_tag "Search", class: "waves-effect waves-light btn" %>
      <% end %>
      
      <% if !user_signed_in? %>
        <li><%= link_to "Log In", new_user_session_path %> </li>
        <li><%= link_to "Sign Up", new_user_registration_path %> </li>
      <% end %>
      
      <% if user_signed_in? %>
        <li>Signed in as <%= "#{current_user.email}" %> </li>
        <li><%= link_to "My Lounge", user_exercises_path(current_user) %> </li>
        <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %> </li>
      <% end %>
    </ul>
  </div>
</nav>
